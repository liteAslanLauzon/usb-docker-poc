[project]
name = "dockerclient"
version = "0.1.0"
description = "Test docker client for Python"
readme = "README.md"
requires-python = ">=3.12.2"
dependencies = [
"opencv-python",
"numpy",
"pypylon",
"requests",]
